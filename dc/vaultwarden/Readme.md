# Set up 

Sign up on your vaultwarden instance.
Download the extension.
Register on the extension by connecting to the custom domain

If you are using host domains (eg: xxx.domain.com yyy.domain.com) and you have different passwords for every subdomain
In french settings (sorry)
Paramètres -> Saisie automatique -> options supplémentaires --> Détection de correspondance URI par défaut : Hôte
